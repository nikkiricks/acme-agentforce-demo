# V2 Build Journal — docs & platform findings

Scoring Salesforce's "robust docs" claim live during the CLI-first rebuild.
Verdicts: **answered** (docs solved it), **partial** (docs + trial-and-error), **failed** (docs wrong/absent).

| Task | Question I needed answered | Doc/source consulted | Verdict | Note |
|---|---|---|---|---|
| 1 | Does `sf project retrieve start -m "AiAuthoringBundle:Acme_Enterprise_2"` pull the agent bundle from the org? | `sf project retrieve start ... --json` output/error | failed | Consistently errors "Entity type 'AiAuthoringBundle' is not available in this api version" (tried project default 64.0, and --api-version overrides of 65.0 and 67.0 — org max). This is despite `sf org list metadata-types` listing AiAuthoringBundle as a valid type for this org. Flow retrieve in the same call succeeds fine, so it's specific to the AiAuthoringBundle entity/retrieve path. |
| 1 | Is `sf agent generate authoring-bundle` (the brief's suggested fallback) a real retrieve of the org's existing bundle? | `sf agent generate authoring-bundle --help` | answered | Docs confirm it *generates* a new authoring bundle from an agent spec YAML (or default boilerplate with `--no-spec`) using an LLM — it does not download the org's actual existing bundle content. Did not run it: doing so would have overwritten the already-present, already-validated `Acme_Enterprise_2.agent` (obtained by a successful retrieve earlier) with fabricated content, violating the retrieve-only/never-edit constraint in spirit. Kept the existing file as baseline. |
| 1 | What are the exact flags to validate an Agent Script authoring bundle locally? | `sf agent validate --help`, `sf agent validate authoring-bundle --help` | answered | Command is `sf agent validate authoring-bundle -n <bundle-api-name> -o <target-org>`. Ran against `Acme_Enterprise_2`: `{"success": true}`, no compile errors. Uses the org connection (not fully offline). |
| 1 | Does the retrieved `Acme_Enterprise_2.agent` already contain a `CountCandidates` action targeting `flow://Count_Candidates`, as later tasks assume? | Full read of `Acme_Enterprise_2.agent` (159 lines) + `grep -i count` | failed | No `CountCandidates` action or `flow://Count_Candidates` reference exists anywhere in the current baseline. The `Count_Candidates` flow exists standalone/unwired. Flagging for whichever task wires it up: there is no existing action definition to mirror verbatim — it will need to be authored from scratch using the flow's actual interface (input `requestNote`: String, output `candidateCount`: Number). |
| 2 | Is the Acme Enterprise agent's version committed/active in the org, or still a draft? | `sf data query -q "SELECT DeveloperName, Id FROM BotDefinition WHERE DeveloperName LIKE 'Acme%'"` then `SELECT BotDefinition.DeveloperName, VersionNumber, Status FROM BotVersion WHERE BotDefinition.DeveloperName LIKE 'Acme%' ORDER BY VersionNumber` | answered | Both queries ran cleanly against `renewal-org`. `BotDefinition` Acme_Enterprise = `0Xxbm0000035JrVCAU` (matches Task 1's agent ID). Two `BotVersion` rows exist — VersionNumber 1 and 2 — **both `Status: Inactive`**. A `sf sobject describe -s BotVersion` shows the `Status` picklist has only two values (`Active`/`Inactive`, default `Inactive`) — there is no separate "Draft" state at this object level. Verdict: **neither version is active/live in the org right now.** The Aug 10 UI session left the agent with an Inactive v2, not a committed/published one. |
| 2 | What's the CLI mechanism to make a version live for Task 5's deploy? | `sf agent publish --help`, `sf agent activate --help` | answered | `sf agent publish authoring-bundle` publishes a bundle to the org, producing a new agent or a new BotVersion of an existing one — but publishing alone does not activate it. `sf agent activate -n <api-name> --version <n> -o <org>` is the separate step that flips a specific BotVersion to `Active` (only one version can be Active at a time; needed before `sf agent preview` works). Task 5's deploy is therefore two CLI steps: publish (commit content) then activate (make it live), not one. |
| 2 | Does a newer, in-org planner/plugin version explain the CountCandidates discrepancy the Aug 10 UI session saw in preview? | Tooling API queries (`--use-tooling-api`) against `GenAiPlannerBundle`/`GenAiPlugin` (brief's suggested names), then `GenAiPlannerDefinition`, `GenAiPluginDefinition`, `GenAiFunctionDefinition` | partial | `GenAiPlannerBundle` and `GenAiPlugin` don't exist as object names (error messages themselves revealed the real names: `GenAiPlannerDefinition`, `GenAiPluginDefinition`) — no doc pointed to this, found by trial-and-error off error text. Once corrected: found two planner definitions, `Acme_Enterprise_v1` (created 2026-08-10T02:48) and `Acme_Enterprise_v2` (created 2026-08-10T21:33Z, i.e. later that same day). `Acme_Enterprise_v2` has an extra topic/plugin, `candidate_screening_16jbm000002JU7h` ("Candidate Screening"), that `v1` does not have — this is new, org-side, and postdates the bundle retrieved in Task 1. However, `GenAiFunctionDefinition` (the action/function object) has only 2 records **in the whole org**, both belonging to the unrelated Renewal Radar agent (`Get_At_Risk_Renewals_Summary`) — zero function records exist under Candidate Screening or anywhere in the Acme Enterprise agent. Verdict: the newer planner version added the *topic container* for candidate screening, but no `CountCandidates` action/function was ever persisted as committed metadata on either planner version. The Aug 10 preview almost certainly exercised an unsaved/transient action in the Agent Builder UI, not something retrievable — confirms Task 1's conclusion that it must be authored from scratch, and rules out "just retrieve the newer version" as a fix. |
| 3 | Do `sf data import bulk` flags match the brief's guessed command (`--sobject`, `--file`, `--wait`)? | `sf data import bulk --help` | answered | Flags matched exactly: `-s/--sobject`, `-f/--file`, `-w/--wait`, plus `--line-ending` (CRLF\|LF) and `--column-delimiter`. No fallback needed — the brief's command as written is valid syntax. |
| 3 | Why did the first `sf data import bulk` run fail with `ClientInputError: LineEnding is invalid on user data. Current LineEnding setting is LF`? | Job failure JSON output, then inspected the CSV bytes directly | partial | Python's `csv.writer` (used to author `candidates-v2.csv`) defaults to `\r\n` (CRLF) line terminators even on macOS when the file is opened with `newline=""` as the csv module docs require. The Bulk API's line-ending auto-detection guessed LF and rejected the CRLF data as a mismatch. Not documented anywhere obvious in `sf data import bulk --help` that auto-detection can misfire — found by inspecting the actual file bytes (`\r\n` count) against the error text. Fix: explicit `--line-ending CRLF` flag, matching the CSV's real line endings. Re-run succeeded: 17 processed, 17 successful, 0 failed. |
| 3 | Why did `scripts/verify-roster.sh` (brief's exact script) throw `JSONDecodeError: Expecting value` when piping `sf data query --json` into `python3 -c "...json.load..."`? | Redirected stdout/stderr separately, then dumped raw bytes with `repr()` | failed | Not a docs gap but a local shell-environment quirk: this session has `FORCE_COLOR=3` set globally, and the installed Node/oclif CLI honors `FORCE_COLOR` over `--json` and even over `NO_COLOR` (Node itself warns "the 'NO_COLOR' env is ignored due to 'FORCE_COLOR' env being set"), so `sf ... --json` emits ANSI-escaped JSON that breaks `json.load`. Fix applied in the script itself: `export FORCE_COLOR=0` at the top of `scripts/verify-roster.sh`, ahead of both `sf` invocations. Confirmed clean, colorless JSON after the override; script then ran exactly as specified in the brief and passed (25 total, Elena Vasquez 79 words). |
| 4 | Does `sf project deploy start -m "Flow:Get_Candidate_Profiles"` accept the brief's flow XML as-is, including the empty `<value><stringValue></stringValue></value>` default on `profilesText`? | `sf project deploy start ... --json` output | answered | Deployed clean on the first attempt — `numberComponentErrors: 0`, `status: Succeeded`. The brief's anticipated trap (empty `stringValue` default needing removal) did not occur; the org's API 64.0 metadata validator accepted it unchanged. One `Info`-level (non-blocking) problem was returned on `componentSuccesses`: a standard warning that `SystemModeWithoutSharing` grants all running users view/edit access to all data — expected per the brief's required `runInMode`, no action needed. |
| 4 | Does `Flow.Interview.createInterview('Get_Candidate_Profiles', ...)` + `iv.start()` correctly execute the deployed flow and expose `profilesText` via `getVariableValue`? | `sf apex run --file scripts/apex/verify-profiles-flow.apex` (brief's exact script, verbatim) | answered | Ran without modification. All four `System.assert` calls passed; debug line read `PROFILE BLOCKS: 25 | LENGTH: 11361`, confirming the roster count from Task 3 (25 Contacts) and both named candidates (Maria Gonzalez, Elena Vasquez) are present in the flow's loop-and-append output. |
| 5 | Does the `candidate_screening` subagent already contain a `CountCandidates` block to mirror, as the brief assumes? | Full read of `Acme_Enterprise_2.agent` (post-Task-1 baseline) | failed | Confirmed (again) zero `actions:` block existed anywhere under `candidate_screening` — no `CountCandidates`, no `GetCandidateProfiles`, nothing to mirror. Per Task-1/Task-2 journal history this was already known; authored both actions from scratch this task, using `renewal_josh`'s `Renewal_Radar_Advisor.agent` (`get_at_risk_renewals`) purely as a style reference for the `actions:`/`reasoning.actions` split, and the brief's own `string` + `is_required`/`is_user_input` template (not the `object`/`complex_data_type_name` style, since no complex-type example existed to copy). |
| 5 | Do the `Count_Candidates` and `Get_Candidate_Profiles` flows actually expose `requestNote` in / `candidateCount` or `profilesText` out, as the action defs assume? | `grep` of both `.flow-meta.xml` files for `<variables>` blocks | answered | Confirmed exactly: both flows declare `requestNote` (String, `isInput: true`); `Count_Candidates` outputs `candidateCount` (Number, `isOutput: true`); `Get_Candidate_Profiles` outputs `profilesText` (String, `isOutput: true`). Action defs written to match verbatim. |
| 5 | Does `sf agent validate authoring-bundle -n Acme_Enterprise_2 -o renewal-org` pass with both new actions added? | `sf agent validate authoring-bundle ... --json` | answered | `{"success": true}` on the first attempt — no compile errors from adding two full action definitions plus the `reasoning.actions`/`reasoning.instructions` wiring. |
| 5 | Does `sf project deploy start -m "AiAuthoringBundle:Acme_Enterprise_2"` deploy the bundle, now that it validates? | `sf project deploy start ... --json` | failed | Same failure mode as Task 1's retrieve: `"Not available for deploy for this API version"` on both `AiAuthoringBundle` components (the `.agent` and `.bundle-meta.xml`). Confirms this metadata type is retrieve/deploy-blocked via `sf project deploy start` in this org/API-version combination — not just retrieve. Moved to the `sf agent publish authoring-bundle` path per the brief's own fallback. |
| 5 | Does `sf agent publish authoring-bundle -n Acme_Enterprise_2 -o renewal-org` work, using the bundle's own folder/`-n` name? | `sf agent publish authoring-bundle ... --json` | failed | `CannotFindBundle: Cannot find an authoring bundle in .../force-app that matches Acme_Enterprise` — note the error names `Acme_Enterprise`, **not** `Acme_Enterprise_2`. Not documented in `--help`; root-caused by reading the installed plugin source (`@salesforce/agents/lib/agents/scriptAgentPublisher.js` → `validateDeveloperName()` and `lib/utils.js` → `findAuthoringBundle()`). The command compiles the `.agent` file, reads `config.developer_name` from inside it (`"Acme_Enterprise"`), then re-derives the bundle's file path as the literal directory `aiAuthoringBundles/<developer_name>` — it does **not** reuse the `-n`/`--api-name` value for this second lookup. Since our bundle folder was named `Acme_Enterprise_2` (a local/Task-1 naming choice) while `config.developer_name` inside the file is `Acme_Enterprise`, publish could never find it, no matter what `-n` was passed. The bundle's own `Acme_Enterprise_2.bundle-meta.xml` already had `<target>Acme_Enterprise.v2</target>`, confirming the "real" linkage was always `Acme_Enterprise`. Fix: `git mv` the folder and both member files from `Acme_Enterprise_2` → `Acme_Enterprise` (filesystem rename only — zero content changes to `config:`/`access:` or any subagent other than `candidate_screening`, confirmed via `git diff -M` showing a detected rename with only the intended diff). Re-validated clean under the new name, then retried publish. |
| 5 | After the rename, does `sf agent publish authoring-bundle -n Acme_Enterprise -o renewal-org` succeed cleanly? | `sf agent publish authoring-bundle ... --verbose --json` (backgrounded, >120s) | partial | Command exited 10 with `MetadataTransferError: Metadata API request failed: fetch failed`, plus a deploy `id` in the error payload — looked like a hard failure. Cross-checked independently: `sf data query` on `BotVersion` showed a brand-new `VersionNumber: 3` (Inactive) had in fact been created; `sf data query --use-tooling-api` on `GenAiFunctionDefinition` showed both `CountCandidates_...` and `GetCandidateProfiles_...` now exist in the org; `sf project deploy report -i <id-from-the-error>` showed that exact deploy as `"status": "Succeeded"`, `numberComponentErrors: 0`, with both `AiAuthoringBundle` components (`.agent`, `.bundle-meta.xml`) in `componentSuccesses`. Verdict: the publish and the metadata deploy it triggers both actually **succeeded** server-side; `fetch failed` was a spurious/transient error in the CLI's own post-deploy status-check network call, not a real failure. Undocumented failure mode — the exit code and top-level error message actively mislead here; only cross-querying the org proved the work had completed. Local retrieve-back also landed cleanly: new `force-app/main/default/bots/Acme_Enterprise/` and `force-app/main/default/genAiPlannerBundles/Acme_Enterprise_v3/` metadata now in the working tree. |
| 5 | What's the exact CLI command to make BotVersion 3 live? | `sf agent activate --help`, then `sf agent activate -n Acme_Enterprise --version 3 -o renewal-org --json` | answered | Matches Task 2's finding exactly: publish alone does not activate. `sf agent activate -n <bot-developer-name> --version <n>` is the separate, final step. Ran clean: `{"success": true, "version": 3}`. Confirmed via `BotVersion` query: v3 `Status: Active`, v1/v2 remain `Inactive` (only one version can be active at a time, as documented). The full author→validate→publish→activate loop **does** stay entirely in the `sf` CLI — the one broken link (`sf project deploy start -m "AiAuthoringBundle:..."`) is fully bypassed by `sf agent publish authoring-bundle`'s own internal deploy step, which uses a different (working) code path for the same metadata type. |
| 6 | Do `sf agent generate test-spec`, `sf agent test create`, `sf agent test run`, `sf agent test results --help` make the test workflow discoverable? | All four `--help` outputs | answered | Genuinely good docs, the best-documented surface of the whole build: each command's description explains the full author→create→run→results lifecycle and cross-references the others by name. `generate test-spec` doc explicitly says it "doesn't look in your org" (uses local DX metadata only) and documents `--from-definition` for round-tripping an existing `AiEvaluationDefinition`/`AiTestingDefinition` back to YAML. `test run --help` documents the `--wait`/no-wait/`resume` split and all 4 `--result-format` options. Only soft spot: none of the four `--help` texts state the exact YAML key names or nesting for `testCases[].customEvaluations` — had to read the installed plugin source (`@salesforce/plugin-agent/lib/commands/agent/generate/test-spec.js`, `@salesforce/agents/lib/agentTest.js`) to get the precise shape, since the brief called for hand-authoring the spec directly (the interactive-only generator doesn't script well from an agent). |
| 6 | Since `sf agent generate test-spec` is interactive-only (no flags to script utterance/topic/action answers), can the exact YAML `TestSpec` shape be reverse-engineered from source instead of running the wizard? | `@salesforce/plugin-agent/lib/commands/agent/generate/test-spec.js` (`promptForTestCase`), `@salesforce/agents/lib/agentTest.js` (`convertToMetadata`/`convertToSpec`) | answered | Yes, cleanly. Confirmed exact top-level shape (`name`, `description`, `subjectType: AGENT`, `subjectName`, `testCases[]`) and per-case keys (`utterance`, `expectedTopic`, `expectedActions[]`, `expectedOutcome`, `customEvaluations[]`, `conversationHistory[]`, `contextVariables[]`). Also confirmed `sf agent test create --spec ... --preview` renders the exact `AiEvaluationDefinition` XML without deploying, which was used to sanity-check the hand-authored YAML before touching the org. |
| 6 | What are the org's current API names for the `candidate_screening` topic and its two actions, to use as `expectedTopic`/`expectedActions` — trusting local metadata, or verifying against the org? | `sf data query --use-tooling-api -q "SELECT DeveloperName FROM GenAiPluginDefinition"` and `...GenAiFunctionDefinition WHERE DeveloperName LIKE '%179bm000003tC5I%'` | partial | Per the task's own gotcha note (Aug 10 UI-era naming may not match agentscript names), queried instead of guessing. Confirmed the org's live `GenAiPluginDefinition`/`GenAiFunctionDefinition` full names exactly match the local `force-app/main/default/genAiPlannerBundles/Acme_Enterprise_v3/...genAiPlannerBundle` file (`candidate_screening_16jbm000002K4hR`, `CountCandidates_179bm000003tC5I`, `GetCandidateProfiles_179bm000003tC5I` — despite a stale duplicate topic set from an older bundle version also present in the org, `*_16jbm000002JU7h`). Verdict **partial**, not **answered**: these *are* the correct full names for the metadata components, but (see next row) they turned out to be the **wrong** values for the test framework's runtime assertions, which compare against the short local developer name instead. Also: `sf project retrieve start -m "GenAiPlannerBundle:..." -m "Bot:..."` failed outright with `UNSUPPORTED_API_VERSION: ... update to package version v65 or higher` even with `--api-version 67.0` explicitly passed (org max) — a real, unexplained retrieve gap for this metadata type, worked around by trusting the already-committed local files (confirmed identical to org state via the Tooling API queries above) instead of re-retrieving. |
| 6 | Does `expectedTopic`/`expectedActions` need the org-suffixed `GenAiPluginDefinition`/`GenAiFunctionDefinition` full name (what the CLI's own interactive generator would supply, per `getPluginsAndFunctions` in the plugin source) or the short local developer name? | First full `sf agent test run --wait 10 --verbose` (job `4KBbm00000032rhGAA`), then `sf agent test results --job-id ... --verbose` after the run's own `--wait` window hit the "fetch failed" gotcha | failed, then answered | First run using the full/suffixed names (`candidate_screening_16jbm000002K4hR`, `CountCandidates_179bm000003tC5I`) came back with `Topic: Error` and `Action: Error` on case 1 (not `Fail` — a parse/lookup error), and case 4's topic assertion was a clean `Fail` with `Actual: candidate_screening` vs `Expected: candidate_screening_16jbm000002K4hR`. This is a genuine, undocumented CLI/framework gap: the interactive `generate test-spec` wizard's own topic/action choice lists (`Object.keys(genAiPlugins)`, `genAiFunctions` — both full/suffixed names read from `GenAiPlannerBundle.localTopicLinks`/`localActionLinks`) do not match what the runtime evaluator actually compares against (`planner_response.lastExecution.topic` / `invokedActions[].function.name`, both short local developer names, confirmed via `--verbose`'s "Generated Data" JSON: `{"function":{"name":"CountCandidates"}}`). Following the wizard's own suggested values as-authored would have produced a permanently-failing suite. Fixed by switching `expectedTopic`/`expectedActions` to the short names (`candidate_screening`, `CountCandidates`, `GetCandidateProfiles`); re-run (job `4KBbm00000032tJGAQ`) came back 100% Topic Pass, 100% Action Pass. |
| 6 | Does a `customEvaluations` (`string_comparison`, `operator: contains`) entry with `actual: $.generatedData.outcome` (`isReference: true`) work for exact-substring checks against the agent's response text, as the `--verbose` help text implies ("use the JSON structure... to build the test case JSONPath expression")? | First test run's `--verbose` output | failed | No — every `customEvaluations` entry referencing `$.generatedData.outcome` came back `Error`, not `Pass`/`Fail`: `"Error parsing JSONPath '$.outputs[?(@.type == 'general.echo')].payload.planner_response.lastExecution.outcome'"`. That path is the server's own internal translation of the `$.generatedData.outcome` shorthand (matches a `ACTUAL_PATH_MAP` entry found in `@salesforce/agents/lib/yamlSpecTranslator.js`, `'$.generatedData.outcome': '{sm.response}'` — a shorthand table that belongs to the newer, not-yet-CLI-wired `agent test run-eval` JSON-payload path, not the `AiEvaluationDefinition`/testing-center path used by `test create`/`run`) — the shorthand is real but the server-side JSONPath it expands to appears to have a quoting bug for this org/API version when used inside a legacy `AiEvaluationDefinition` `customEvaluation`. Not fixed — no working `actual` JSONPath was found for response-text `customEvaluations` in the time available. Worked around per the brief's own explicit guidance ("if the framework supports semantic/judge expectations, use those; journal it"): dropped all `customEvaluations`, folded every "response must contain X" requirement into the natural-language `expectedOutcome` field instead, which is scored by the framework's built-in LLM-judge `bot_response_rating` scorer and worked correctly (3 of 4 cases scored `Outcome: Pass` on the re-run, with the 4th being a real agent-behavior finding, not a scoring artifact — see `docs/demo-tests.md`). |
| 6 | Does `sf agent test run --wait 10`'s exit/error behavior match the "spurious fetch failed" pattern already seen from `sf agent publish` in Task 5? | First run's terminal output, then `sf agent test results --job-id <id> --verbose` on the same job | answered | Yes, same failure mode. After ~7m40s of polling inside the 10-minute wait window, the command errored `Error (TestPollFailed): Failed to poll test results: fetch failed` (exit 10) — looked like a hard failure. Cross-checked per the task's own standing gotcha: `sf agent test results --job-id 4KBbm00000032rhGAA -o renewal-org --verbose` immediately returned the full, complete result set for that job (all 4 cases scored, `Status: ERROR` — a real result, not a polling artifact, since 2 of 4 test cases came back empty/unrendered even in the full fetch, which is a separate, likely display-layer bug in `--result-format human` for cases with certain error shapes). Confirms: **never trust a `sf agent test run --wait` exit code that errors** — always re-fetch by job ID before concluding the suite failed to run. The corrected second run (job `4KBbm00000032tJGAQ`) completed cleanly with no error at all, so this isn't universal — it seems tied to whichever run needs longer polling or hits more `Error`-status test cases. |
| 7 | Does the `Minimum Access - Salesforce` profile actually exist in this org, or does the brief's fallback (query `Profile`, pick the most restrictive) need to trigger? | `sf data query -q "SELECT Id, Name FROM Profile WHERE Name = 'Minimum Access - Salesforce'"` | answered | Exists cleanly — one record returned (`00ebm00000MDYvOAAX`). No fallback needed; the brief's `SecurityBeatTest.cls` code was usable verbatim for `newUserWithNoGrantsSeesNothing`. |
| 7 | Does the brief's baked-in correction — method-level `@IsTest(SeeAllData=true)` on `declaredSystemContextSeesTheRoster` only, leaving `newUserWithNoGrantsSeesNothing` plain `@IsTest` in the same class — compile and behave as described (flow interview sees the 25 org Contacts; the zero-privilege test stays data-independent)? | `sf project deploy start -m "ApexClass:SecurityBeatTest"` then `sf apex run test --tests SecurityBeatTest --result-format human --wait 10` | answered | Deployed clean (`numberComponentErrors: 0`) and ran clean on the first attempt — no compile error, no mixed-DML/SeeAllData surprise. Both tests passed: `declaredSystemContextSeesTheRoster` (433 ms) confirms the flow's declared `SystemModeWithoutSharing` run context returns the full org roster even though the test method itself inserts no data; `newUserWithNoGrantsSeesNothing` (569 ms) confirms a bare `Minimum Access - Salesforce` user querying `Contact WITH USER_MODE` sees nothing. 2/2 pass, `Test Run Id 707bm000018WlF9`. No fallback needed — the brief's correction as written was sufficient. |
| 7 | Was `newUserWithNoGrantsSeesNothing`, as written verbatim from the brief, actually proving anything about least-privilege enforcement? | Code review (Critical finding, post-implementation) | failed | No — it was vacuous. Without `SeeAllData`, an Apex test method sees zero pre-existing records for *any* user, admin included, so `Database.query('SELECT Id FROM Contact WITH USER_MODE')` was guaranteed to return an empty list (or throw on object-access denial) regardless of whether real FLS/CRUD security was enforced for the bare user — `blocked` was true by construction and the `Assert.isTrue` could never fail. The test would have passed identically even if the `Minimum Access - Salesforce` profile secretly had full Contact read. Caught in review, not by any doc — this is exactly the class of bug the passing-but-meaningless test represents, which is why the build journal exists to record it. Fixed by inserting a real `Contact` as the admin test user *before* `System.runAs(bare)` (test-created data is visible in test context without `SeeAllData`), adding a positive-control assertion that the admin can see it, then inside `runAs(bare)` asserting the specific inserted record's Id is absent from what the bare user sees (or that the query throws outright) — so a real security regression (bare user gaining Contact read) would now make the test fail. Redeployed and reran: 2/2 pass, new `Test Run Id 707bm000018W2DZ`. |
| 8 | For the runbook's "agent test framework is unusable" fallback, does `sf agent preview` need a separately linked/connected client app, or is `-n <api-name>` on an activated agent enough? | `sf agent preview --help` | answered | `--help` documents the full flag set clearly: `-n/--api-name` is "API name of the activated published agent you want to interact with," `--authoring-bundle` is the alternative for previewing a *local* Agent Script file before publishing, and `--use-live-actions` is documented as the opt-in for real (vs. AI-simulated/mocked) actions. The default-is-mocked behavior is the important, well-documented detail — an unqualified `sf agent preview` would demo simulated flow calls, which is exactly the wrong thing to show a customer. No connected-app setup is mentioned or needed for an activated agent.
| 8 | For re-fetching a prior suite's results in the runbook, does `sf agent test results` support `--use-most-recent` as an alternative to hunting down the job ID? | `sf agent test results --help`, then running the flag | failed | The `--help` output is self-contradictory. Its DESCRIPTION says "You can also use the --use-most-recent flag to see..." and two of its EXAMPLES are literally `sf agent test results --use-most-recent --target-org my-org` — but the flag appears nowhere in the FLAGS list, and USAGE marks `-i, --job-id` as `(required)`. Running it: `Error (2): Nonexistent flag: --use-most-recent` (CLI 2.145.6). The command's own documented examples do not run. Corrected `docs/demo-tests.md`, which had repeated the phantom flag from this same help text. |
| 9 | Does `sf agent test create --spec ... --force-overwrite` enforce a length limit on the spec's top-level `description` field, and is it documented? | `sf agent test create --help`, then the command's own error output | failed | Not documented anywhere in `--help` (no mention of `AiEvaluationDefinition.description`'s 255-char cap, or any field-length constraints at all). First attempt at an updated, more descriptive `testCases` spec description errored server-side: `Evaluation Type: data value too large: ... (max length=255)`. Fixed by shortening the description to 213 chars. Found only by running the command and reading the raw `SfError` message — `--help` gives zero warning. |
| 9 | Does editing `agent_router`'s `reasoning.instructions` text (the brief's literal, narrowly-scoped fix for a router misclassification) actually change `model://sfdc_ai__DefaultEinsteinHyperClassifier`'s live routing decision? | 4 consecutive publish→activate→`sf agent test run` cycles (BotVersions 4, 5, 6, 7), each adding one more textual signal (router instructions, `candidate_screening`'s top-level `description`, a `description:` on the `go_to_candidate_screening` transition action, a negative `description:` on `go_to_off_topic`) | failed, then answered | All four cycles, tested immediately after activation, reproduced the identical bug: "Draft a short outreach email to Elena Vasquez" (case 5, cold-start, no `conversationHistory`) still classified as `off_topic` with zero invoked actions and the identical generic response, regardless of which of the four textual levers had just been added. Per `superpowers:systematic-debugging`'s "3+ failed fixes ⇒ question the architecture" rule, stopped guessing at wording and re-tested with a temporary 6th diagnostic test case (`Draft a short candidate outreach email to Elena Vasquez.`, never committed) against the *same already-active* BotVersion 7 — no new publish — several minutes after the four failed attempts. It passed, and immediately re-running the real case 5 utterance (identical text to all four prior failed attempts) on the same BotVersion also passed. Verdict: the root cause was **asynchronous reindexing lag in the HyperClassifier after publish/activate**, not insufficient or misplaced instruction text — the CLI's publish/activate commands return success the instant the metadata deploy completes, but the classifier's own decision boundary visibly took several more minutes to catch up, and nothing in `sf agent publish`/`sf agent activate --help` output or `--json` result warns of this. Kept all four textual edits (they match Salesforce's own documented `description:`-on-transition-action pattern, found via `developer.salesforce.com/docs/ai/agentforce/guide/ascript-patterns-topic-selector.html` — "This description helps the reasoning engine decide when to delegate to the subagent") since they're additive, harmless, and may still contribute alongside the lag; the diff is therefore broader than the brief's literal "agent_router instructions and candidate_screening instructions only" guard by two lines (a `description:` field on `candidate_screening` itself, and two `description:` lines on `agent_router`'s transition actions) — flagged here explicitly as a deliberate, evidence-based deviation, not an oversight. `access:`, action *definitions*, and all other subagents remained untouched throughout. |
| 9 | Does the org expose a mechanism for topic/router training utterances (as opposed to free-text descriptions) for the HyperClassifier, and does Agent Script's DSL support authoring them? | `sf sobject describe -s AiPluginUtteranceDef --use-tooling-api`, `sf data query` against it for every plugin on the current planner, `developer.salesforce.com/docs/ai/agentforce/guide/ascript-reference.html` | answered | Yes, such an object exists (`AiPluginUtteranceDef`, child of `GenAiPluginDefinition`, with a plain `Utterance` textarea field) — but it has **zero rows** for every topic on every planner version of this agent, and the Agent Script reference page (fetched in full) documents no YAML key for authoring sample/training utterances at all. Confirms the DSL currently only exposes description-level text as a router-classification lever; utterance-level training, if it matters for this classifier, isn't reachable from Agent Script or the `sf agent` CLI surface at all. |

## Final scorecard — did "robust docs" hold?

**33 questions asked of the docs across 9 tasks.**

| Verdict | Count | Share |
|---|---|---|
| **answered** — docs solved it outright | 17 | 52% |
| **partial** — docs plus trial-and-error | 4 | 12% |
| **failed** — docs wrong, absent, or actively misleading | 10 | 30% |
| **failed, then answered** — docs sent me the wrong way, empirical result corrected it | 2 | 6% |

Two of the nine failures deserve an asterisk in Salesforce's favor: the
`verify-roster.sh` JSON parse failure (Task 3) was a local `FORCE_COLOR=3`
shell quirk, not a documentation gap, and the vacuous-security-test failure
(Task 7) was a defect in *this build's own plan*, caught by code review. Netting
those out, **seven hard documentation failures remain — and all seven are on the
`sf agent` / `AiAuthoringBundle` surface.**

**Split by surface age:**

| Surface | Rows | answered | partial | failed |
|---|---|---|---|---|
| `sf agent` + `AiAuthoringBundle` (Tasks 1, 2, 5, 6, 8) | 22 | 11 | 3 | 7 (+1 hybrid) |
| Flows, Bulk API data, Apex (Tasks 3, 4, 7) | 8 | 5 | 1 | 2 (neither a real docs gap) |

### Best doc moment

`sf agent test` (Task 6, row 20). All four commands — `generate test-spec`,
`test create`, `test run`, `test results` — describe the full lifecycle in their
own `--help` text *and cross-reference each other by name*, so reading any one
of them tells you the next command to run. `generate test-spec --help` even
volunteers a non-obvious scoping fact ("doesn't look in your org") before you
can trip over it. This is the newest surface in the entire build and it has the
best documentation in the entire build — which cuts against the simple
"new = undocumented" story. (Caveat, found in Task 8: the same otherwise-
excellent `test results --help` documents a `--use-most-recent` flag in its own
examples that does not exist in the shipped command. Best-in-build still ships a
copy-paste example that errors.)

### Worst doc moment

`sf agent publish authoring-bundle` and `sf agent test run --wait` both **exit
non-zero with `fetch failed` on runs that fully succeeded server-side** (Task 5,
rows 25/18; Task 6, row 32). The publish that reported failure had in fact
created BotVersion 3, registered both `GenAiFunctionDefinition` records, and
logged a `Succeeded` deploy — provable only by cross-querying the org with the
deploy ID buried in the *error* payload. No `--help` text, error message, or doc
warns of this. An exit code that lies is worse than an undocumented flag: it is
the one signal a CI pipeline is built to trust, and here it would have failed
every green build.

Runner-up, and arguably worse for a first-time user: the CLI's own interactive
`generate test-spec` wizard offers org-suffixed topic/action names
(`candidate_screening_16jbm000002K4hR`) that the runtime evaluator can never
match, because it compares against short local developer names (Task 6, row 30).
Following the tool's own suggestions produces a permanently-failing test suite.

### The one-sentence verdict

"Robust docs" held completely for the mature surfaces — flows, Bulk API imports,
and Apex produced zero genuine documentation failures across eight questions and
every command worked as written the first time — while the newest surface,
`sf agent` / `AiAuthoringBundle`, is a split personality: excellently documented
where it is documented (the test lifecycle), and silent exactly where it hurts
most (metadata-type deploy gaps, the folder-name-equals-developer-name rule,
and success reported as failure), such that seven of the twenty-two questions on
that surface were answerable only by reading the installed plugin's source, by
cross-querying the org to find out what had actually happened, or by running a
documented example and watching it error.

### Task 9 addendum: the silent gap gets a new entry — classifier reindexing lag

Task 9's live-preview bug fix (routing "draft an outreach email" requests to
`candidate_screening` instead of `off_topic`) added a new instance of the
worst-doc-moment pattern above, one layer deeper: `sf agent publish` and `sf
agent activate` both report success the instant their metadata deploy lands,
but the `model://sfdc_ai__DefaultEinsteinHyperClassifier` router model backing
`agent_router` took several more minutes to actually start honoring the
change — four consecutive, identically-configured publish→activate→test
cycles reproduced the pre-fix bug before a fifth attempt (no new publish,
just elapsed time) finally passed. No command's `--help` or `--json` output
warns that "activated" and "the router now reflects your edit" are different
moments in time. This cost the most iteration time of any finding in the
build and is exactly the kind of gap `docs/build-journal.md` exists to
surface — see row 9 above for the full investigation.
