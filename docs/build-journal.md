# V2 Build Journal — docs & platform findings

Scoring Salesforce's "robust docs" claim live during the CLI-first rebuild.
Verdicts: **answered** (docs solved it), **partial** (docs + trial-and-error), **failed** (docs wrong/absent).

| Task | Question I needed answered | Doc/source consulted | Verdict | Note |
|---|---|---|---|---|
| 1 | Does `sf project retrieve start -m "AiAuthoringBundle:Acme_Enterprise_2"` pull the agent bundle from the org? | `sf project retrieve start ... --json` output/error | failed | Consistently errors "Entity type 'AiAuthoringBundle' is not available in this api version" (tried project default 64.0, and --api-version overrides of 65.0 and 67.0 — org max). This is despite `sf org list metadata-types` listing AiAuthoringBundle as a valid type for this org. Flow retrieve in the same call succeeds fine, so it's specific to the AiAuthoringBundle entity/retrieve path. |
| 1 | Is `sf agent generate authoring-bundle` (the brief's suggested fallback) a real retrieve of the org's existing bundle? | `sf agent generate authoring-bundle --help` | answered | Docs confirm it *generates* a new authoring bundle from an agent spec YAML (or default boilerplate with `--no-spec`) using an LLM — it does not download the org's actual existing bundle content. Did not run it: doing so would have overwritten the already-present, already-validated `Acme_Enterprise_2.agent` (obtained by a successful retrieve earlier) with fabricated content, violating the retrieve-only/never-edit constraint in spirit. Kept the existing file as baseline. |
| 1 | What are the exact flags to validate an Agent Script authoring bundle locally? | `sf agent validate --help`, `sf agent validate authoring-bundle --help` | answered | Command is `sf agent validate authoring-bundle -n <bundle-api-name> -o <target-org>`. Ran against `Acme_Enterprise_2`: `{"success": true}`, no compile errors. Uses the org connection (not fully offline). |
| 1 | Does the retrieved `Acme_Enterprise_2.agent` already contain a `CountCandidates` action targeting `flow://Count_Candidates`, as later tasks assume? | Full read of `Acme_Enterprise_2.agent` (159 lines) + `grep -i count` | failed | No `CountCandidates` action or `flow://Count_Candidates` reference exists anywhere in the current baseline. The `Count_Candidates` flow exists standalone/unwired. Flagging for whichever task wires it up: there is no existing action definition to mirror verbatim — it will need to be authored from scratch using the flow's actual interface (input `requestNote`: String, output `candidateCount`: Number). |
| 2 | Is the Acme Enterprise agent's version committed/active in the org, or still a draft? | `sf data query -q "SELECT DeveloperName, Id FROM BotDefinition WHERE DeveloperName LIKE 'Acme%'"` then `SELECT BotDefinition.DeveloperName, VersionNumber, Status FROM BotVersion WHERE BotDefinition.DeveloperName LIKE 'Acme%' ORDER BY VersionNumber` | answered | Both queries ran cleanly against `renewal-org`. `BotDefinition` Acme_Enterprise = `0Xxbm0000035JrVCAU` (matches Task 1's agent ID). Two `BotVersion` rows exist — VersionNumber 1 and 2 — **both `Status: Inactive`**. A `sf sobject describe -s BotVersion` shows the `Status` picklist has only two values (`Active`/`Inactive`, default `Inactive`) — there is no separate "Draft" state at this object level. Verdict: **neither version is active/live in the org right now.** The Aug 10 UI session left the agent with an Inactive v2, not a committed/published one. |
| 2 | What's the CLI mechanism to make a version live for Task 5's deploy? | `sf agent publish --help`, `sf agent activate --help` | answered | `sf agent publish authoring-bundle` publishes a bundle to the org, producing a new agent or a new BotVersion of an existing one — but publishing alone does not activate it. `sf agent activate -n <api-name> --version <n> -o <org>` is the separate step that flips a specific BotVersion to `Active` (only one version can be Active at a time; needed before `sf agent preview` works). Task 5's deploy is therefore two CLI steps: publish (commit content) then activate (make it live), not one. |
| 2 | Does a newer, in-org planner/plugin version explain the CountCandidates discrepancy the Aug 10 UI session saw in preview? | Tooling API queries (`--use-tooling-api`) against `GenAiPlannerBundle`/`GenAiPlugin` (brief's suggested names), then `GenAiPlannerDefinition`, `GenAiPluginDefinition`, `GenAiFunctionDefinition` | partial | `GenAiPlannerBundle` and `GenAiPlugin` don't exist as object names (error messages themselves revealed the real names: `GenAiPlannerDefinition`, `GenAiPluginDefinition`) — no doc pointed to this, found by trial-and-error off error text. Once corrected: found two planner definitions, `Acme_Enterprise_v1` (created 2026-08-10T02:48) and `Acme_Enterprise_v2` (created 2026-08-10T21:33Z, i.e. later that same day). `Acme_Enterprise_v2` has an extra topic/plugin, `candidate_screening_16jbm000002JU7h` ("Candidate Screening"), that `v1` does not have — this is new, org-side, and postdates the bundle retrieved in Task 1. However, `GenAiFunctionDefinition` (the action/function object) has only 2 records **in the whole org**, both belonging to the unrelated Renewal Radar agent (`Get_At_Risk_Renewals_Summary`) — zero function records exist under Candidate Screening or anywhere in the Acme Enterprise agent. Verdict: the newer planner version added the *topic container* for candidate screening, but no `CountCandidates` action/function was ever persisted as committed metadata on either planner version. The Aug 10 preview almost certainly exercised an unsaved/transient action in the Agent Builder UI, not something retrievable — confirms Task 1's conclusion that it must be authored from scratch, and rules out "just retrieve the newer version" as a fix. |
| 3 | Do `sf data import bulk` flags match the brief's guessed command (`--sobject`, `--file`, `--wait`)? | `sf data import bulk --help` | answered | Flags matched exactly: `-s/--sobject`, `-f/--file`, `-w/--wait`, plus `--line-ending` (CRLF\|LF) and `--column-delimiter`. No fallback needed — the brief's command as written is valid syntax. |
| 3 | Why did the first `sf data import bulk` run fail with `ClientInputError: LineEnding is invalid on user data. Current LineEnding setting is LF`? | Job failure JSON output, then inspected the CSV bytes directly | partial | Python's `csv.writer` (used to author `candidates-v2.csv`) defaults to `\r\n` (CRLF) line terminators even on macOS when the file is opened with `newline=""` as the csv module docs require. The Bulk API's line-ending auto-detection guessed LF and rejected the CRLF data as a mismatch. Not documented anywhere obvious in `sf data import bulk --help` that auto-detection can misfire — found by inspecting the actual file bytes (`\r\n` count) against the error text. Fix: explicit `--line-ending CRLF` flag, matching the CSV's real line endings. Re-run succeeded: 17 processed, 17 successful, 0 failed. |
| 3 | Why did `scripts/verify-roster.sh` (brief's exact script) throw `JSONDecodeError: Expecting value` when piping `sf data query --json` into `python3 -c "...json.load..."`? | Redirected stdout/stderr separately, then dumped raw bytes with `repr()` | failed | Not a docs gap but a local shell-environment quirk: this session has `FORCE_COLOR=3` set globally, and the installed Node/oclif CLI honors `FORCE_COLOR` over `--json` and even over `NO_COLOR` (Node itself warns "the 'NO_COLOR' env is ignored due to 'FORCE_COLOR' env being set"), so `sf ... --json` emits ANSI-escaped JSON that breaks `json.load`. Fix applied in the script itself: `export FORCE_COLOR=0` at the top of `scripts/verify-roster.sh`, ahead of both `sf` invocations. Confirmed clean, colorless JSON after the override; script then ran exactly as specified in the brief and passed (25 total, Elena Vasquez 79 words). |
